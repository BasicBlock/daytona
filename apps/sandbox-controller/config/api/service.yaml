apiVersion: v1
kind: Service
metadata:
  name: sandbox-api
  namespace: daytona-system
  labels:
    app.kubernetes.io/name: sandbox-api
spec:
  selector:
    app.kubernetes.io/name: sandbox-api
  ports:
    - name: http
      port: 80
      targetPort: http

